-
 name: pb on use case of command modele
 hosts: client1
 tasks:
  - name: code to create a folder
    command:
     cmd: mkdir /opt/Russia
  - name: code to create a file under that folder
    command:
     cmd: touch /opt/Russia/sample1
  - name: code to remove git application
    command:
     cmd: yum remove -y git
